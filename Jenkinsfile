pipeline {
  agent{
  }
  parameters{
      string(defaultValue: "git-link", description: "enter the name", name: "deploymentName")
  }
  stages{
    stage('bruh ')
      steps{
        sh 'git clone ${params.git}'
      }
   }
}
